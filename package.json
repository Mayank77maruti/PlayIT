{
  "name": "playit",
  "version": "1.0.0",
  "description": "A game where you spin the three buttons and get points accordingly.",
  "main": "play.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "prompt-sync": "^4.2.0"
  }
}
